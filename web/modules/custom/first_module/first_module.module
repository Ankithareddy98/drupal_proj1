<?php

/**
 * @file
 * Primary module hooks for first_module module.
 */
/**
 * Implements hook_theme()
 */
function first_module_theme($existing, $type, $theme, $path) {
  return [
    'first_module_template' => [
        'variables' => [
            'first_msg' => 'Something',
            'items' => [],
        ],
        'template' => 'first-module-template',
    ],
  ];

}