<div class="hello-world">
  <h1>{{ first_msg }}</h1>
</div>

<div class="basic-pages">
  {% for item in items %}
    <div class="basic-page">
      <h2>{{ item.title }}</h2>
     <h4>{{ item.body }}</h4>
      <p>Created on : {{ item.date }}</p>
    </div>
  {% endfor %}
</div> 